<%= form_with(model: domaine, local: true) do |form| %>
  <% if domaine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(domaine.errors.count, "error") %> prohibited this domaine from being saved:</h2>

      <ul>
      <% domaine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :user_id, :value => current_user.id %>
  </div>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
